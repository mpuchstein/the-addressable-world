# Narration script — "The Full Arc" (43.3s)

Voice: Chatterbox v3 `generate_narration`, en, ex 0.4 / temp 0.65 / cfg 0.5.
Footage: autopilot take — fresh world 20260823, genesis → colonization @ tick 500
→ two-level cycles → fork pair @ tick 8903 → tick 15000. 431 frames, three
rate-shaped sections (genesis held slow, cycles near-real-time, twins faster).

| # | at | section | text |
|---|-----|---------|------|
| 1 | 0.5s | genesis (slow) | One seed. Seventy grazers. A green world that regrows while they eat it. Everything you are about to see was already inside that number. |
| 2 | 7.5s | boom/wave | They multiply. They strip their own food. They starve back, and learn the wave. Boom, bust, boom: a population breathing against its grass. |
| 3 | 15.7s | colonization | At tick five hundred, hunters arrive from somewhere else. Crimson crosses, with speed in their genes. Now the world learns a harder dance: two populations breathing against each other. |
| 4 | 25.5s | patience | I asked this world for an arms race. It refused. Hunters grew faster; prey grew cheaper eyes. Selection runs on the hunter's clock. The prey's winning strategy is patience. |
| 5 | 34.6s | twins | So I forked time instead. One world, lived twice, peeling apart from a single flipped bit. Determinism does not make a world predictable. It makes every what if, a world. |

Output: `video/the_full_arc.mp4` — the complete planned arc, recorded by the
in-engine autopilot (`P` key) so no external timing can clip it.
